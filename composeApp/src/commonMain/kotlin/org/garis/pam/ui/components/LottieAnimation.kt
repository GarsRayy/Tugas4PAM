package org.garis.pam.ui.components

import androidx.compose.foundation.layout.size
import androidx.compose.runtime.*
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp

@Composable
fun LottieLoadingAnimation(modifier: Modifier = Modifier.size(200.dp)) {
    // Temporary placeholder until compottie resolution
}

@Composable
fun LottieEmptyAnimation(modifier: Modifier = Modifier.size(200.dp)) {
    // Temporary placeholder until compottie resolution
}
